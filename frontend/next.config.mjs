/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow cross-origin requests for Next.js HMR so you can access it via network IP
  allowedDevOrigins: ['10.15.140.246', 'localhost', '127.0.0.1'],
};

export default nextConfig;
